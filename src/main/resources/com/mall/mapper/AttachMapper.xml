<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.mapper.AttachMapper">

	<select id="getAttachList"
		resultType="com.mall.model.AttachImageVO">

		select * from vam_image where itemId = #{itemId}

	</select>

</mapper>